import { Template } from 'meteor/templating';
import { Session } from 'meteor/session';

/*import './templates/clock/clock';
import './templates/countersList/counters';
import './templates/addExpDateForm/add_date_form';*/

/*import './layout.html';*/

/*Template.layout.onRendered(() => {
    Session.set('mainTemplate', 'countersList');
});

Template.layout.helpers({
    getMainTemplate: () => {
        return Session.get('mainTemplate');
    }
});*/

/*
Template.layout.events({
    'click .add_event_modal'()  {
        Session.set('mainTemplate', 'addExpDateForm');
    }
});
*/

